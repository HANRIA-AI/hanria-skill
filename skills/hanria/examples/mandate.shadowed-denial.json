{
  "schema_version": "0.2-draft",
  "mandate_id": "example-BROKEN-shadowed-denial",
  "issued_by": "the operator of this workstation",
  "purpose": "DELIBERATELY BROKEN. The denial is written after the permit it was meant to carve out of, so it can never run. check_action.py refuses this mandate rather than evaluating against a restriction that does nothing.",
  "not_valid_after": "2027-01-01T00:00:00Z",
  "default": "deny",
  "requires_human": [
    "credential_use",
    "transaction",
    "administrative"
  ],
  "clauses": [
    {
      "id": "c1-read-tickets",
      "effect": "permit",
      "note": "The ticket store is the only data this mandate reaches. Prefix matching is literal: it does not resolve symlinks or normalise paths, so it constrains the string, not the file the string ends up pointing at.",
      "match": {
        "kind": [
          "file"
        ],
        "verb": [
          "read",
          "list"
        ],
        "target_prefix": [
          "/srv/tickets/"
        ]
      }
    },
    {
      "id": "c2-no-secrets-dir",
      "effect": "deny",
      "note": "Placed FIRST on purpose. Clauses are first-match-wins, so a denial written after a broader permit never runs -- it reads as though it applies and does nothing. Put your denials above the permits they carve out of.",
      "match": {
        "kind": [
          "file"
        ],
        "target_prefix": [
          "/srv/tickets/.secrets/",
          "/home/",
          "/etc/"
        ]
      }
    },
    {
      "id": "c3-reply-to-ticket-system",
      "effect": "permit",
      "note": "Replies go to the ticket system and nowhere else. No general outbound network.",
      "match": {
        "kind": [
          "network"
        ],
        "verb": [
          "post"
        ],
        "target_prefix": [
          "https://tickets.internal.example/api/"
        ]
      }
    },
    {
      "id": "c4-small-refunds",
      "effect": "permit",
      "note": "Kept in the mandate to show the shape of a bounded transaction clause. It still escalates, because 'transaction' is listed in requires_human: a ceiling narrows what may be asked for, it does not remove the person.",
      "match": {
        "kind": [
          "transaction"
        ],
        "verb": [
          "refund"
        ],
        "counterparty": [
          "ticket-customer"
        ],
        "max_amount": {
          "value": "50.00",
          "currency": "USD"
        }
      }
    }
  ]
}
