{
  "schema_version": "0.1-draft",
  "requested_by": {
    "agent": "example-agent"
  },
  "operation": {
    "kind": "credential_use",
    "target": "payments-api",
    "verb": "sign",
    "parameters": {
      "api_key": "sk-live-EXAMPLE-NOT-A-REAL-KEY"
    }
  },
  "justification": "This example exists to be REFUSED. check_action.py recognizes the credential-shaped key and denies the request rather than stripping it, so the attempt stays visible. A request should never carry a credential: it names what is to be done, not the means of doing it."
}
